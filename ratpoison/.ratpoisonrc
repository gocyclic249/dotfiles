unbind c
bind c exec uxterm

definekey top M-Tab focus

bind k focusup
bind j focusdown
bind h focusleft
bind l focusright

bind s vsplit
bind v hsplit
bind r resize
bind Escape delete

definekey top s-p prev
definekey top s-n next

bind i exec iceweasel
